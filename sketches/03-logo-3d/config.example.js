/* Template. Copy this to config.js and fill in real values.
   config.js is gitignored, so the key never lands in the repo. */
window.AN_CONFIG = {
  calendarId: "your-calendar-id@group.calendar.google.com",
  apiKey: "your-google-calendar-api-key",
};
